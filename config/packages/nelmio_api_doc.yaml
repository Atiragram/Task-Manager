nelmio_api_doc:
    documentation:
        info:
            title: Task Manager
            description:
            version: 1.0.0
        securityDefinitions:
            Bearer:
                type: apiKey
                description: 'Value: Bearer token'
                name: Authorization
                in: header
            security:
                - Bearer: []

    areas:
        path_patterns:
            - ^/api(?!/doc$)
